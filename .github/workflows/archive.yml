name: Archive Perth Priority Removals

on:
  workflow_dispatch:
  schedule:
    - cron: "0 17 1 * *"

jobs:
  archive:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.2"
          bundler-cache: true

      - name: Install dependencies
        run: bundle install

      - name: Test homepage fetch
        run: |
          echo "Normal HEAD request"
          curl -I https://perthpriorityremovals.com.au
          echo ""
          echo "Internet Archive user-agent HEAD request"
          curl -A "ia_archiver" -I https://perthpriorityremovals.com.au

      - name: Run Wayback archiver
        run: bundle exec ruby script/archive_ppr.rb
